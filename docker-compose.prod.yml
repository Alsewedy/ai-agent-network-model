services:
  ai-agent-prod:
    image: ai-agent-network-model:${APP_VERSION}
    container_name: ai-agent-prod
    env_file:
      - .env.production
    ports:
      - "8002:8000"
    restart: unless-stopped
